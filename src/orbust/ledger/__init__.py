"""Experiment Ledger & Model Store — track runs, store artifacts, enable comparison.

Components:
    experiments:    Experiment tracking, SQLite schema, run lifecycle
    model_store:    Directory layout, save/load with PyTorch state_dict
"""
