"""Strategy definitions — one subdirectory per strategy.

Each strategy directory contains:
    strategy.py     Strategy class implementing the Strategy ABC
    config.yaml     Strategy-specific configuration (features, model, hyperparams)
"""
